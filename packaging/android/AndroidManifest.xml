<?xml version="1.0" encoding="utf-8"?>
<!-- Thin WebView shell around the WASM build (dist/web bundled as assets).
     The game itself is GOOS=js; the shell serves it from
     https://appassets.androidplatform.net/assets/ via request
     interception (fetch() on file:// origins is blocked by Chromium,
     and the WASM needs a real https origin to load + reach Supabase). -->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.daviey.mario">

  <uses-permission android:name="android.permission.INTERNET" />

  <application
      android:label="Mario"
      android:icon="@mipmap/ic_launcher"
      android:allowBackup="true"
      android:theme="@style/AppTheme">

    <activity
        android:name=".MainActivity"
        android:exported="true"
        android:launchMode="singleTask"
        android:screenOrientation="sensorLandscape"
        android:configChanges="orientation|screenSize|screenLayout|smallestScreenSize|keyboardHidden|uiMode|density">
      <intent-filter>
        <action android:name="android.intent.action.MAIN" />
        <category android:name="android.intent.category.LAUNCHER" />
      </intent-filter>
    </activity>
  </application>
</manifest>
